-r requirements.txt
pytest>=8.0,<10.0
# Client HTTP recommandé par Starlette pour le TestClient de FastAPI.
httpx2>=2.13,<3.0
# Ruff ajoute des règles et ajuste son formatage à chaque version mineure (0.x) :
# on fige la mineure pour que la CI ne casse pas sans changement de code.
ruff>=0.16,<0.17
mypy>=2.0,<3.0
types-httplib2>=0.32,<1.0
